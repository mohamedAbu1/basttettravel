export const metadata = {
  title: "Booking Result | Basttet Travel",
  robots: { index: false, follow: false },
};

export default function CheckoutSuccessLayout({ children }) {
  return children;
}
